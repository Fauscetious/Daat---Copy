import { AxiosInstance } from 'axios';
export interface DadJoke {
    id: string;
    joke: string;
    status?: string;
}
export interface DadJokeSearchResponse {
    current_page: number;
    limit: number;
    next_page: number;
    previous_page: number;
    results: DadJoke[];
    search_tearm: string;
    status: number;
    total_jokes: number;
    total_pages: number;
}
export declare class ICanHazDadJokeClient {
    client: AxiosInstance;
    constructor(baseURL?: string);
    /**
     * Fetch a random dad joke
     */
    getRandomJoke(): Promise<DadJoke>;
    /**
     * Fetch a dad joke
     * @param id key used to look up joke
     */
    getJokeById(id: string): Promise<DadJoke>;
    /**
     * Search for dad jokes
     * @param page page number to start result set
     * @param limit number of items in result set
     * @param term term to find within joke text
     */
    search(page?: number, limit?: number, term?: string): Promise<DadJokeSearchResponse>;
}
